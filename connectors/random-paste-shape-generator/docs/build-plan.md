# Build plan

Interpret the user request "random paste shape genearot" as a tiny local MCP tool that returns one random paste-ready shape.

## Tool
- `get_random_paste_shape`

## Formats
- `emoji` (default)
- `ascii`
- `text`
